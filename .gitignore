# Dependencies
node_modules/

# Secrets
.env

# Per-server local state (created automatically at runtime, not code)
modlogs-config.json
access-config.json
tickets-config.json
tickets-state.json
verify-config.json
verify-state.json

# Saved code snippets (per-guild user data, not code)
codigos/

# OS/editor cruft
.DS_Store

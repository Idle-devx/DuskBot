node_modules/

# Secrets — never commit your real .env, only .env.example
.env

# Runtime state: per-guild config, ticket/verification/anti-raid state,
# and saved code snippets. All of it lives under data/ now (see
# src/lib/constants.js) instead of being scattered next to the source
# files, specifically so this one line covers all of it.
/data/
